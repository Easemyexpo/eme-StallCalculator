<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Universal Exhibition Cost Calculator 2025</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        
        .header {
            background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }
        
        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
            font-weight: 300;
        }
        
        .header p {
            font-size: 1.1em;
            opacity: 0.9;
        }
        
        .calculator-body {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 0;
        }
        
        .form-section {
            padding: 40px;
            background: #f8f9fa;
        }
        
        .summary-section {
            padding: 40px;
            background: #2c3e50;
            color: white;
            position: sticky;
            top: 0;
            height: fit-content;
        }
        
        .input-category {
            background: white;
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 25px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
            border-left: 4px solid #3498db;
        }
        
        .category-title {
            font-size: 1.4em;
            font-weight: 600;
            color: #2c3e50;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
        }
        
        .input-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 20px;
        }
        
        .input-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }
        
        .input-group.full-width {
            grid-column: 1 / -1;
        }
        
        .input-group.triple {
            grid-column: 1 / -1;
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            gap: 15px;
            align-items: end;
        }
        
        .input-group label {
            font-weight: 500;
            color: #555;
            font-size: 0.95em;
        }
        
        .input-group input, .input-group select {
            padding: 12px 15px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 1em;
            transition: all 0.3s ease;
            background: #fafafa;
        }
        
        .input-group input:focus, .input-group select:focus {
            outline: none;
            border-color: #3498db;
            background: white;
            box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.1);
        }
        
        .checkbox-group {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            margin-top: 10px;
        }
        
        .checkbox-item {
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .checkbox-item input[type="checkbox"] {
            width: auto;
            margin: 0;
        }
        
        .summary-card {
            background: rgba(255,255,255,0.1);
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 20px;
            backdrop-filter: blur(10px);
        }
        
        .summary-title {
            font-size: 1.3em;
            font-weight: 600;
            margin-bottom: 20px;
            color: #ecf0f1;
        }
        
        .summary-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 12px;
            padding-bottom: 8px;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }
        
        .summary-item.total {
            border-top: 2px solid #3498db;
            border-bottom: none;
            padding-top: 15px;
            margin-top: 15px;
            font-size: 1.2em;
            font-weight: 700;
        }
        
        .summary-item.grand-total {
            background: rgba(46, 204, 113, 0.2);
            margin: 20px -25px -25px -25px;
            padding: 20px 25px;
            border-radius: 0 0 15px 15px;
            font-size: 1.4em;
            font-weight: 700;
            color: #2ecc71;
        }
        
        .cost-breakdown {
            background: rgba(255,255,255,0.05);
            padding: 15px;
            border-radius: 10px;
            margin: 15px 0;
            font-size: 0.9em;
        }
        
        .currency-settings {
            background: rgba(52, 152, 219, 0.1);
            border-left: 3px solid #3498db;
            padding: 15px;
            margin: 20px 0;
            border-radius: 5px;
            font-size: 0.9em;
            color: #2c3e50;
        }
        
        .warning-box {
            background: rgba(230, 126, 34, 0.1);
            border-left: 3px solid #e67e22;
            padding: 15px;
            margin: 20px 0;
            border-radius: 5px;
            font-size: 0.9em;
            color: #d35400;
        }
        
        .reset-btn {
            width: 100%;
            padding: 15px;
            background: #e74c3c;
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 1.1em;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 20px;
        }
        
        .reset-btn:hover {
            background: #c0392b;
            transform: translateY(-2px);
        }
        
        .info-icon {
            cursor: help;
            margin-left: 5px;
            color: #3498db;
        }
        
        .tooltip {
            position: relative;
            display: inline-block;
        }
        
        .tooltip .tooltiptext {
            visibility: hidden;
            width: 300px;
            background-color: #555;
            color: #fff;
            text-align: center;
            border-radius: 6px;
            padding: 10px;
            position: absolute;
            z-index: 1;
            top: -5px;
            right: 105%;
            font-size: 0.85em;
            opacity: 0;
            transition: opacity 0.3s;
        }
        
        .tooltip:hover .tooltiptext {
            visibility: visible;
            opacity: 1;
        }
        
        @media (max-width: 768px) {
            .calculator-body {
                grid-template-columns: 1fr;
            }
            
            .summary-section {
                position: static;
            }
            
            .input-row, .input-group.triple {
                grid-template-columns: 1fr;
                gap: 15px;
            }
            
            .header h1 {
                font-size: 2em;
            }
            
            .tooltip .tooltiptext {
                right: -50px;
                width: 250px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Universal Exhibition Cost Calculator 2025</h1>
            <p>Plan your exhibition budget worldwide - supports multiple currencies and international venues</p>
        </div>
        
        <div class="calculator-body">
            <div class="form-section">
                <div class="currency-settings">
                    💡 <strong>Currency & Location:</strong> This calculator works globally. Set your currency and enter local rates. All estimates are based on industry averages and can be adjusted for your specific market.
                </div>

                <div class="input-category">
                    <h3 class="category-title">🌍 Global Settings</h3>
                    <div class="input-row">
                        <div class="input-group">
                            <label>Currency</label>
                            <select id="currency">
                                <option value="USD">USD ($)</option>
                                <option value="EUR">EUR (€)</option>
                                <option value="GBP">GBP (£)</option>
                                <option value="CAD">CAD (C$)</option>
                                <option value="AUD">AUD (A$)</option>
                                <option value="JPY">JPY (¥)</option>
                                <option value="CNY">CNY (¥)</option>
                                <option value="INR">INR (₹)</option>
                                <option value="SGD">SGD (S$)</option>
                                <option value="CHF">CHF (Fr)</option>
                            </select>
                        </div>
                        <div class="input-group">
                            <label>Market Cost Level</label>
                            <select id="market-level">
                                <option value="low">Low Cost Market (Emerging)</option>
                                <option value="medium" selected>Medium Cost Market (Standard)</option>
                                <option value="high">High Cost Market (Premium Cities)</option>
                                <option value="premium">Ultra Premium (NYC, London, Tokyo)</option>
                            </select>
                        </div>
                    </div>
                </div>

                <div class="input-category">
                    <h3 class="category-title">🎯 Event Details</h3>
                    <div class="input-row">
                        <div class="input-group">
                            <label>Event Type</label>
                            <select id="event-type">
                                <option value="trade">Trade Show</option>
                                <option value="consumer">Consumer Exhibition</option>
                                <option value="tech">Technology Conference</option>
                                <option value="medical">Medical/Healthcare</option>
                                <option value="food">Food & Beverage</option>
                                <option value="automotive">Automotive</option>
                                <option value="fashion">Fashion/Beauty</option>
                                <option value="industrial">Industrial/Manufacturing</option>
                                <option value="other">Other Industry</option>
                            </select>
                        </div>
                        <div class="input-group">
                            <label>Event Duration (days)</label>
                            <input type="number" id="event-duration" min="1" max="10" value="3" placeholder="3">
                        </div>
                    </div>
                    <div class="input-row">
                        <div class="input-group">
                            <label>Distance from Company (km)</label>
                            <input type="number" id="distance" min="0" max="20000" value="500" placeholder="500">
                        </div>
                        <div class="input-group">
                            <label>Venue Type</label>
                            <select id="venue-type">
                                <option value="standard">Standard Convention Center</option>
                                <option value="premium">Premium Venue</option>
                                <option value="hotel">Hotel Conference Center</option>
                                <option value="outdoor">Outdoor/Temporary</option>
                            </select>
                        </div>
                    </div>
                </div>

                <div class="input-category">
                    <h3 class="category-title">🏗️ Booth Specifications</h3>
                    <div class="input-row">
                        <div class="input-group">
                            <label>Booth Size (sq meters)</label>
                            <select id="booth-size">
                                <option value="9">Small (9 sq m / 100 sq ft)</option>
                                <option value="18">Medium (18 sq m / 200 sq ft)</option>
                                <option value="36">Large (36 sq m / 400 sq ft)</option>
                                <option value="54">X-Large (54 sq m / 600 sq ft)</option>
                                <option value="84">XX-Large (84 sq m / 900 sq ft)</option>
                                <option value="custom">Custom Size</option>
                            </select>
                        </div>
                        <div class="input-group">
                            <label>Custom Size (if selected)</label>
                            <input type="number" id="custom-size" min="1" max="1000" placeholder="Enter sq meters">
                        </div>
                    </div>
                    <div class="input-row">
                        <div class="input-group">
                            <label>Booth Type</label>
                            <select id="booth-type">
                                <option value="shell">Shell Scheme (Basic)</option>
                                <option value="modular">Modular System</option>
                                <option value="octonorm">Octonorm System</option>
                                <option value="custom">Custom Build</option>
                                <option value="rental">Premium Rental</option>
                            </select>
                        </div>
                        <div class="input-group">
                            <label>Booth Position</label>
                            <select id="booth-position">
                                <option value="inline">In-line (Standard)</option>
                                <option value="corner">Corner (+25%)</option>
                                <option value="island">Island (+50%)</option>
                                <option value="peninsula">Peninsula (+35%)</option>
                            </select>
                        </div>
                    </div>
                    <div class="input-group full-width">
                        <label>Additional Features</label>
                        <div class="checkbox-group">
                            <div class="checkbox-item">
                                <input type="checkbox" id="premium-flooring">
                                <label for="premium-flooring">Premium Flooring</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="special-lighting">
                                <label for="special-lighting">Special Lighting</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="furniture-rental">
                                <label for="furniture-rental">Furniture Package</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="storage-space">
                                <label for="storage-space">Storage Space</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="meeting-room">
                                <label for="meeting-room">Private Meeting Room</label>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="input-category">
                    <h3 class="category-title">👥 Team & Accommodation</h3>
                    <div class="input-row">
                        <div class="input-group">
                            <label>Staff Attending</label>
                            <input type="number" id="staff-count" min="1" max="50" value="2" placeholder="2">
                        </div>
                        <div class="input-group">
                            <label>Accommodation Level</label>
                            <select id="accommodation-level">
                                <option value="budget">Budget (Hostel/2-3 star)</option>
                                <option value="business">Business (3-4 star)</option>
                                <option value="luxury">Luxury (4-5 star)</option>
                                <option value="ultra">Ultra Luxury (5+ star)</option>
                            </select>
                        </div>
                    </div>
                    <div class="input-row">
                        <div class="input-group">
                            <label>Accommodation Nights</label>
                            <input type="number" id="accommodation-nights" min="1" max="15" value="4" placeholder="4">
                        </div>
                        <div class="input-group">
                            <label>Travel Method</label>
                            <select id="travel-method">
                                <option value="flight">Flight</option>
                                <option value="train">Train/Rail</option>
                                <option value="car">Car/Drive</option>
                                <option value="bus">Bus/Coach</option>
                            </select>
                        </div>
                    </div>
                    <div class="input-group triple">
                        <div>
                            <label>Daily Meal Budget (per person)</label>
                            <input type="number" id="meal-budget" min="0" max="500" value="75" placeholder="75">
                        </div>
                        <div>
                            <label>Local Transport Budget (total)</label>
                            <input type="number" id="local-transport" min="0" max="2000" value="200" placeholder="200">
                        </div>
                        <div>
                            <label>Incidentals (per person/day)</label>
                            <input type="number" id="incidentals" min="0" max="200" value="30" placeholder="30">
                        </div>
                    </div>
                </div>

                <div class="input-category">
                    <h3 class="category-title">📦 Shipping & Materials</h3>
                    <div class="warning-box">
                        ⚠️ <strong>Material Handling (Drayage):</strong> This is often the most expensive surprise cost! Factor 60-400 per 100kg depending on your location.
                    </div>
                    <div class="input-row">
                        <div class="input-group">
                            <label>Total Material Weight (kg)
                                <span class="tooltip info-icon">ℹ️
                                    <span class="tooltiptext">Include booth materials, graphics, products, promotional items, and equipment. Higher weight = higher drayage costs.</span>
                                </span>
                            </label>
                            <input type="number" id="material-weight" min="0" max="5000" value="100" placeholder="100">
                        </div>
                        <div class="input-group">
                            <label>Shipping Method</label>
                            <select id="shipping-method">
                                <option value="standard">Standard Ground/Sea</option>
                                <option value="expedited">Expedited</option>
                                <option value="freight">Air Freight</option>
                                <option value="courier">Express Courier</option>
                            </select>
                        </div>
                    </div>
                    <div class="input-row">
                        <div class="input-group">
                            <label>Material Handling Rate (per 100kg)
                                <span class="tooltip info-icon">ℹ️
                                    <span class="tooltiptext">Drayage cost to move materials from venue dock to your booth. In India: ~₹12,000/100kg. Check venue service kit for exact rates.</span>
                                </span>
                            </label>
                            <input type="number" id="drayage-rate" min="0" max="1000" placeholder="Auto (varies by currency)">
                        </div>
                        <div class="input-group">
                            <label>Number of Shipments</label>
                            <select id="shipment-count">
                                <option value="1" selected>Single Shipment (Recommended)</option>
                                <option value="2">Two Shipments</option>
                                <option value="3">Three Shipments</option>
                                <option value="4">Four+ Shipments</option>
                            </select>
                        </div>
                    </div>
                    <div class="input-group full-width">
                        <label>Marketing Materials</label>
                        <div class="checkbox-group">
                            <div class="checkbox-item">
                                <input type="checkbox" id="printed-materials">
                                <label for="printed-materials">Printed Materials (Brochures, Cards)</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="promotional-items">
                                <label for="promotional-items">Promotional Giveaways</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="signage-graphics">
                                <label for="signage-graphics">Signage & Graphics</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="product-samples">
                                <label for="product-samples">Product Samples</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="digital-materials">
                                <label for="digital-materials">Digital/Interactive Materials</label>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="input-category">
                    <h3 class="category-title">💻 Technology & Services</h3>
                    <div class="input-row">
                        <div class="input-group">
                            <label>Internet Package</label>
                            <select id="internet-package">
                                <option value="none">No Internet</option>
                                <option value="basic">Basic WiFi</option>
                                <option value="premium">High-Speed WiFi</option>
                                <option value="dedicated">Dedicated Connection</option>
                            </select>
                        </div>
                        <div class="input-group">
                            <label>Electrical Package</label>
                            <select id="electrical-package">
                                <option value="basic">Basic Power</option>
                                <option value="enhanced">Enhanced Package</option>
                                <option value="heavy">Heavy Duty</option>
                                <option value="custom">Custom Requirements</option>
                            </select>
                        </div>
                    </div>
                    <div class="input-group full-width">
                        <label>Additional Technology Services</label>
                        <div class="checkbox-group">
                            <div class="checkbox-item">
                                <input type="checkbox" id="av-rental">
                                <label for="av-rental">A/V Equipment Rental</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="led-screens">
                                <label for="led-screens">LED Screens/Video Walls</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="sound-system">
                                <label for="sound-system">Sound System</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="interactive-tech">
                                <label for="interactive-tech">Interactive Technology</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="live-streaming">
                                <label for="live-streaming">Live Streaming Setup</label>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="input-category">
                    <h3 class="category-title">⚙️ Labor & Installation</h3>
                    <div class="warning-box">
                        ⚠️ <strong>Union Labor:</strong> Many venues require certified labor for setup/breakdown. Check local requirements to avoid fines.
                    </div>
                    <div class="input-row">
                        <div class="input-group">
                            <label>Labor Rate (per hour)
                                <span class="tooltip info-icon">ℹ️
                                    <span class="tooltiptext">Local labor rates vary significantly. In India: ~₹800/hour. Include overtime rates if setup is during premium hours.</span>
                                </span>
                            </label>
                            <input type="number" id="labor-rate" min="0" max="500" placeholder="Auto (varies by currency)">
                        </div>
                        <div class="input-group">
                            <label>Estimated Setup/Breakdown Hours</label>
                            <input type="number" id="labor-hours" min="0" max="100" value="12" placeholder="12">
                        </div>
                    </div>
                    <div class="input-row">
                        <div class="input-group">
                            <label>Union Labor Required?</label>
                            <select id="union-required">
                                <option value="no">No Union Requirements</option>
                                <option value="partial">Some Union Requirements</option>
                                <option value="full">Full Union Venue</option>
                            </select>
                        </div>
                        <div class="input-group">
                            <label>Setup Timing</label>
                            <select id="setup-timing">
                                <option value="standard">Standard Hours</option>
                                <option value="overtime">Overtime Hours (+50%)</option>
                                <option value="weekend">Weekend/Holiday (+100%)</option>
                            </select>
                        </div>
                    </div>
                    <div class="input-group full-width">
                        <label>Additional Services</label>
                        <div class="checkbox-group">
                            <div class="checkbox-item">
                                <input type="checkbox" id="security-service">
                                <label for="security-service">Security Service</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="cleaning-service">
                                <label for="cleaning-service">Daily Cleaning</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="technical-support">
                                <label for="technical-support">On-site Technical Support</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="hospitality-service">
                                <label for="hospitality-service">Hospitality/Catering</label>
                            </div>
                            <div class="checkbox-item">
                                <input type="checkbox" id="lead-capture">
                                <label for="lead-capture">Lead Capture System</label>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="summary-section">
                <div class="summary-card">
                    <h3 class="summary-title">💰 Cost Breakdown</h3>
                    <div class="summary-item">
                        <span>Booth Space & Design:</span>
                        <span id="booth-cost">0</span>
                    </div>
                    <div class="summary-item">
                        <span>Travel & Accommodation:</span>
                        <span id="travel-cost">0</span>
                    </div>
                    <div class="summary-item">
                        <span>Shipping & Logistics:</span>
                        <span id="shipping-cost">0</span>
                    </div>
                    <div class="summary-item">
                        <span>Material Handling (Drayage):</span>
                        <span id="drayage-cost">0</span>
                    </div>
                    <div class="summary-item">
                        <span>Labor & Installation:</span>
                        <span id="labor-cost">0</span>
                    </div>
                    <div class="summary-item">
                        <span>Technology & Services:</span>
                        <span id="technology-cost">0</span>
                    </div>
                    <div class="summary-item">
                        <span>Marketing Materials:</span>
                        <span id="marketing-cost">0</span>
                    </div>
                    <div class="summary-item">
                        <span>Additional Services:</span>
                        <span id="additional-cost">0</span>
                    </div>
                    <div class="summary-item total">
                        <span>Subtotal:</span>
                        <span id="subtotal">0</span>
                    </div>
                </div>

                <div class="summary-card">
                    <h3 class="summary-title">🛡️ Risk Management</h3>
                    <div class="input-group">
                        <label>Contingency Level</label>
                        <select id="contingency-rate">
                            <option value="0.15">15% (Minimum Recommended)</option>
                            <option value="0.20" selected>20% (Standard Practice)</option>
                            <option value="0.25">25% (Conservative)</option>
                            <option value="0.30">30% (High Risk Events)</option>
                        </select>
                    </div>
                    <div class="summary-item">
                        <span>Contingency Fund:</span>
                        <span id="contingency-amount">0</span>
                    </div>
                    <div class="summary-item grand-total">
                        <span>TOTAL BUDGET:</span>
                        <span id="total-budget">0</span>
                    </div>
                </div>

                <div class="cost-breakdown">
                    <strong>📊 Industry Benchmarks:</strong><br>
                    • Space typically = 33% of total budget<br>
                    • Travel & accommodation = 18%<br>
                    • Services & labor = 25%<br>
                    • Materials & shipping = 15%<br>
                    • Contingency = 20% (recommended)<br><br>
                    <strong>🇮🇳 India-Specific Pricing:</strong><br>
                    • Octonorm: ₹12,000/sq meter (your input)<br>
                    • Labor: ~₹800/hour average<br>
                    • Drayage: ~₹120/100kg<br>
                    • Auto-converts for other currencies<br><br>
                    <strong>💡 Cost-Saving Tips:</strong><br>
                    • Ship as one consolidated shipment<br>
                    • Book early to avoid rush charges<br>
                    • Choose lightweight booth materials<br>
                    • Verify union requirements in advance
                </div>

                <button class="reset-btn" onclick="resetCalculator()">🔄 Reset Calculator</button>
            </div>
        </div>
    </div>

    <script>
        // Global currency settings
        let currentCurrency = 'USD';
        let currencySymbols = {
            'USD': '$', 'EUR': '€', 'GBP': '£', 'CAD': 'C$', 'AUD': 'A$',
            'JPY': '¥', 'CNY': '¥', 'INR': '₹', 'SGD': 'S$', 'CHF': 'Fr'
        };

        // Market cost multipliers
        const marketMultipliers = {
            'low': 0.6,
            'medium': 1.0,
            'high': 1.4,
            'premium': 1.8
        };

        // Base costs by market and currency
        const baseCosts = {
            boothRates: {
                // Global rates in USD (will be converted)
                'shell': 120, 'modular': 180, 'custom': 300, 'rental': 220, 'octonorm': 200,
                // India-specific rates in INR per sq meter
                'india': {
                    'shell': 8000, 'modular': 10000, 'custom': 18000, 'rental': 14000, 'octonorm': 12000
                }
            },
            positionMultipliers: {
                'inline': 1.0, 'corner': 1.25, 'island': 1.5, 'peninsula': 1.35
            },
            accommodationRates: {
                'budget': 60, 'business': 140, 'luxury': 280, 'ultra': 450,
                // India-specific rates in INR per night
                'india': {
                    'budget': 2500, 'business': 6000, 'luxury': 12000, 'ultra': 25000
                }
            },
            travelRates: {
                'flight': 0.25, 'train': 0.15, 'car': 0.08, 'bus': 0.05,
                // India rates per km in INR
                'india': {
                    'flight': 8, 'train': 3, 'car': 6, 'bus': 2
                }
            },
            laborRates: {
                'default': 125,
                'india': 800 // INR per hour for Indian market
            },
            drayageRates: {
                'default': 150,
                'india': 120 // INR per 100kg in India
            }
        };

        function updateCurrency() {
            currentCurrency = document.getElementById('currency').value;
            calculateTotals();
        }

        function getBoothSize() {
            const sizeSelect = document.getElementById('booth-size').value;
            if (sizeSelect === 'custom') {
                return parseFloat(document.getElementById('custom-size').value) || 36;
            }
            return parseFloat(sizeSelect);
        }

        function calculateBoothCost() {
            const boothSize = getBoothSize();
            const boothType = document.getElementById('booth-type').value;
            const position = document.getElementById('booth-position').value;
            const marketLevel = document.getElementById('market-level').value;
            
            let baseRate;
            
            // Use India-specific pricing for INR currency
            if (currentCurrency === 'INR') {
                baseRate = baseCosts.boothRates.india[boothType] || 12000; // Default to octonorm rate
            } else {
                baseRate = baseCosts.boothRates[boothType] || 180;
            }
            
            let positionMultiplier = baseCosts.positionMultipliers[position] || 1.0;
            let marketMultiplier = marketMultipliers[marketLevel] || 1.0;
            
            let baseCost = boothSize * baseRate * positionMultiplier;
            
            // Apply market multiplier only for non-INR currencies (India rates are already localized)
            if (currentCurrency !== 'INR') {
                baseCost *= marketMultiplier;
            }
            
            // Add feature costs (scaled appropriately for currency)
            let featureCost = 0;
            let featureMultiplier = currentCurrency === 'INR' ? 80 : 1; // INR feature multiplier
            
            if (document.getElementById('premium-flooring').checked) featureCost += boothSize * 25 * featureMultiplier;
            if (document.getElementById('special-lighting').checked) featureCost += boothSize * 35 * featureMultiplier;
            if (document.getElementById('furniture-rental').checked) featureCost += boothSize * 40 * featureMultiplier;
            if (document.getElementById('storage-space').checked) featureCost += 800 * featureMultiplier;
            if (document.getElementById('meeting-room').checked) featureCost += 1500 * featureMultiplier;
            
            return baseCost + featureCost;
        }

        function calculateTravelCost() {
            const distance = parseFloat(document.getElementById('distance').value) || 0;
            const staffCount = parseInt(document.getElementById('staff-count').value) || 1;
            const accommodationNights = parseInt(document.getElementById('accommodation-nights').value) || 4;
            const accommodationLevel = document.getElementById('accommodation-level').value;
            const travelMethod = document.getElementById('travel-method').value;
            const marketLevel = document.getElementById('market-level').value;
            const eventDuration = parseInt(document.getElementById('event-duration').value) || 3;
            const mealBudget = parseFloat(document.getElementById('meal-budget').value) || 75;
            const localTransport = parseFloat(document.getElementById('local-transport').value) || 200;
            const incidentals = parseFloat(document.getElementById('incidentals').value) || 30;
            
            let totalCost = 0;
            let marketMultiplier = (currentCurrency === 'INR') ? 1.0 : (marketMultipliers[marketLevel] || 1.0);
            
            // Travel costs
            let travelRate;
            if (currentCurrency === 'INR') {
                travelRate = baseCosts.travelRates.india[travelMethod] || 3;
            } else {
                travelRate = baseCosts.travelRates[travelMethod] || 0.15;
            }
            totalCost += distance * travelRate * staffCount * 2; // Round trip
            
            // Accommodation
            let accommodationRate;
            if (currentCurrency === 'INR') {
                accommodationRate = baseCosts.accommodationRates.india[accommodationLevel] || 6000;
            } else {
                accommodationRate = baseCosts.accommodationRates[accommodationLevel] || 140;
            }
            totalCost += accommodationRate * accommodationNights * staffCount;
            
            // Meals and incidentals - scale for currency
            let currencyMultiplier = currentCurrency === 'INR' ? 80 : 1;
            totalCost += (mealBudget + incidentals) * eventDuration * staffCount * currencyMultiplier;
            totalCost += localTransport * currencyMultiplier;
            
            return totalCost * marketMultiplier;
        }

        function calculateShippingCost() {
            const distance = parseFloat(document.getElementById('distance').value) || 0;
            const materialWeight = parseFloat(document.getElementById('material-weight').value) || 0;
            const shippingMethod = document.getElementById('shipping-method').value;
            const marketLevel = document.getElementById('market-level').value;
            
            if (materialWeight === 0) return 0;
            
            let marketMultiplier = marketMultipliers[marketLevel] || 1.0;
            let shippingRates = {
                'standard': 2.5, 'expedited': 4.0, 'freight': 6.5, 'courier': 8.0
            };
            
            let baseCost = materialWeight * shippingRates[shippingMethod];
            let distanceCost = distance * 0.02 * materialWeight;
            
            return (baseCost + distanceCost) * 2 * marketMultiplier; // Round trip
        }

        function calculateDrayageCost() {
            const materialWeight = parseFloat(document.getElementById('material-weight').value) || 0;
            let drayageRate = parseFloat(document.getElementById('drayage-rate').value);
            const shipmentCount = parseInt(document.getElementById('shipment-count').value) || 1;
            const marketLevel = document.getElementById('market-level').value;
            
            if (materialWeight === 0) return 0;
            
            // Use India-specific default rate if not specified
            if (!drayageRate) {
                drayageRate = currentCurrency === 'INR' ? baseCosts.drayageRates.india : baseCosts.drayageRates.default;
            }
            
            let marketMultiplier = (currentCurrency === 'INR') ? 1.0 : (marketMultipliers[marketLevel] || 1.0);
            
            // Calculate CWT (per 100kg units)
            let cwt = Math.ceil(materialWeight / 100);
            let totalDrayage = cwt * drayageRate * shipmentCount * 2; // In and out
            
            // Minimum charge per shipment (scaled for currency)
            let minimumCharge = (currentCurrency === 'INR' ? 15000 : 200) * shipmentCount * 2;
            
            return Math.max(totalDrayage, minimumCharge) * marketMultiplier;
        }

        function calculateLaborCost() {
            let laborRate = parseFloat(document.getElementById('labor-rate').value);
            const laborHours = parseFloat(document.getElementById('labor-hours').value) || 12;
            const setupTiming = document.getElementById('setup-timing').value;
            const unionRequired = document.getElementById('union-required').value;
            const marketLevel = document.getElementById('market-level').value;
            
            // Use India-specific default rate if not specified
            if (!laborRate) {
                laborRate = currentCurrency === 'INR' ? baseCosts.laborRates.india : baseCosts.laborRates.default;
            }
            
            let marketMultiplier = (currentCurrency === 'INR') ? 1.0 : (marketMultipliers[marketLevel] || 1.0);
            let timingMultiplier = 1.0;
            let unionMultiplier = 1.0;
            
            // Timing multipliers
            if (setupTiming === 'overtime') timingMultiplier = 1.5;
            if (setupTiming === 'weekend') timingMultiplier = 2.0;
            
            // Union multipliers (less relevant in India)
            if (unionRequired === 'partial') unionMultiplier = currentCurrency === 'INR' ? 1.1 : 1.2;
            if (unionRequired === 'full') unionMultiplier = currentCurrency === 'INR' ? 1.2 : 1.5;
            
            return laborRate * laborHours * timingMultiplier * unionMultiplier * marketMultiplier;
        }

        function calculateTechnologyCost() {
            const internetPackage = document.getElementById('internet-package').value;
            const electricalPackage = document.getElementById('electrical-package').value;
            const eventDuration = parseInt(document.getElementById('event-duration').value) || 3;
            const marketLevel = document.getElementById('market-level').value;
            
            let cost = 0;
            let marketMultiplier = marketMultipliers[marketLevel] || 1.0;
            
            // Internet costs
            const internetCosts = { 'none': 0, 'basic': 200, 'premium': 500, 'dedicated': 1200 };
            cost += (internetCosts[internetPackage] || 0);
            
            // Electrical costs
            const electricalCosts = { 'basic': 300, 'enhanced': 600, 'heavy': 1000, 'custom': 1500 };
            cost += (electricalCosts[electricalPackage] || 0);
            
            // Additional tech services
            if (document.getElementById('av-rental').checked) cost += 800 * eventDuration;
            if (document.getElementById('led-screens').checked) cost += 1500 * eventDuration;
            if (document.getElementById('sound-system').checked) cost += 400 * eventDuration;
            if (document.getElementById('interactive-tech').checked) cost += 1000 * eventDuration;
            if (document.getElementById('live-streaming').checked) cost += 2000;
            
            return cost * marketMultiplier;
        }

        function calculateMarketingCost() {
            const marketLevel = document.getElementById('market-level').value;
            let cost = 0;
            let marketMultiplier = marketMultipliers[marketLevel] || 1.0;
            
            if (document.getElementById('printed-materials').checked) cost += 800;
            if (document.getElementById('promotional-items').checked) cost += 1500;
            if (document.getElementById('signage-graphics').checked) cost += 1200;
            if (document.getElementById('product-samples').checked) cost += 600;
            if (document.getElementById('digital-materials').checked) cost += 2000;
            
            return cost * marketMultiplier;
        }

        function calculateAdditionalCost() {
            const eventDuration = parseInt(document.getElementById('event-duration').value) || 3;
            const staffCount = parseInt(document.getElementById('staff-count').value) || 1;
            const marketLevel = document.getElementById('market-level').value;
            
            let cost = 0;
            let marketMultiplier = marketMultipliers[marketLevel] || 1.0;
            
            if (document.getElementById('security-service').checked) cost += 150 * eventDuration;
            if (document.getElementById('cleaning-service').checked) cost += 100 * eventDuration;
            if (document.getElementById('technical-support').checked) cost += 200 * eventDuration;
            if (document.getElementById('hospitality-service').checked) cost += 300 * eventDuration;
            if (document.getElementById('lead-capture').checked) cost += 500;
            
            return cost * marketMultiplier;
        }

        function calculateTotals() {
            const boothCost = calculateBoothCost();
            const travelCost = calculateTravelCost();
            const shippingCost = calculateShippingCost();
            const drayageCost = calculateDrayageCost();
            const laborCost = calculateLaborCost();
            const technologyCost = calculateTechnologyCost();
            const marketingCost = calculateMarketingCost();
            const additionalCost = calculateAdditionalCost();
            
            const subtotal = boothCost + travelCost + shippingCost + drayageCost + 
                           laborCost + technologyCost + marketingCost + additionalCost;
            
            const contingencyRate = parseFloat(document.getElementById('contingency-rate').value) || 0.20;
            const contingencyAmount = subtotal * contingencyRate;
            const totalBudget = subtotal + contingencyAmount;
            
            // Update display
            document.getElementById('booth-cost').textContent = formatCurrency(boothCost);
            document.getElementById('travel-cost').textContent = formatCurrency(travelCost);
            document.getElementById('shipping-cost').textContent = formatCurrency(shippingCost);
            document.getElementById('drayage-cost').textContent = formatCurrency(drayageCost);
            document.getElementById('labor-cost').textContent = formatCurrency(laborCost);
            document.getElementById('technology-cost').textContent = formatCurrency(technologyCost);
            document.getElementById('marketing-cost').textContent = formatCurrency(marketingCost);
            document.getElementById('additional-cost').textContent = formatCurrency(additionalCost);
            document.getElementById('subtotal').textContent = formatCurrency(subtotal);
            document.getElementById('contingency-amount').textContent = formatCurrency(contingencyAmount);
            document.getElementById('total-budget').textContent = formatCurrency(totalBudget);
        }

        function formatCurrency(amount) {
            const symbol = currencySymbols[currentCurrency] || '$';
            
            if (currentCurrency === 'JPY' || currentCurrency === 'CNY') {
                return symbol + Math.round(amount).toLocaleString();
            }
            
            return symbol + amount.toLocaleString('en-US', {
                minimumFractionDigits: 0,
                maximumFractionDigits: 0
            });
        }

        function resetCalculator() {
            if (confirm('Are you sure you want to reset all values?')) {
                // Reset to defaults
                document.getElementById('currency').value = 'USD';
                document.getElementById('market-level').value = 'medium';
                document.getElementById('event-duration').value = '3';
                document.getElementById('distance').value = '500';
                document.getElementById('booth-size').value = '36';
                document.getElementById('staff-count').value = '2';
                document.getElementById('accommodation-nights').value = '4';
                document.getElementById('material-weight').value = '100';
                document.getElementById('labor-hours').value = '12';
                document.getElementById('meal-budget').value = '75';
                document.getElementById('local-transport').value = '200';
                document.getElementById('incidentals').value = '30';
                document.getElementById('contingency-rate').value = '0.20';
                
                // Reset selects to first option
                const selects = document.querySelectorAll('select:not(#currency):not(#market-level):not(#contingency-rate)');
                selects.forEach(select => select.selectedIndex = 0);
                
                // Uncheck all checkboxes
                const checkboxes = document.querySelectorAll('input[type="checkbox"]');
                checkboxes.forEach(checkbox => checkbox.checked = false);
                
                currentCurrency = 'USD';
                calculateTotals();
            }
        }

        // Add event listeners
        document.addEventListener('DOMContentLoaded', function() {
            const inputs = document.querySelectorAll('input, select');
            inputs.forEach(input => {
                input.addEventListener('change', function() {
                    if (this.id === 'currency') {
                        updateCurrency();
                    } else {
                        calculateTotals();
                    }
                });
                input.addEventListener('input', calculateTotals);
            });
            
            // Initial calculation
            calculateTotals();
        });
    </script>
</body>
</html>